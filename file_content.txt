int findMaxElement(int r[], unsigned int size) {
    int max_el = r[0];
    for (unsigned int i = 0; i < size; i++) {
        if (max_el < r[i]) {
            max_el = r[i];
        }
    }
    return max_el;
}

