#include <iostream>

int main() {
    std::vector<int> numbers = {5, 10, 3, 8, 15};
    int result = findLargestVariable(numbers);
    std::cout << "The largest variable in the vector is: " << result << std::endl;

    return 0;
}
